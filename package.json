{
  "name": "julianehendershot",
  "description": "julianehendershot",
  "version": "0.0.1",
  "scripts": {
    "dev": "NODE_ENV='development' sapper dev",
    "build": "NODE_ENV='production' sapper build",
    "export": "NODE_ENV='production' sapper export",
    "start": "node dist",
    "now-build": "npm run export && mv __sapper__/export dist"
  },
  "dependencies": {
    "compression": "^1.7.1",
    "polka": "^0.5.0",
    "sirv": "^0.4.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "sapper": "^0.27.0",
    "svelte": "^3.0.0",
    "svelte-loader": "^2.9.0",
    "webpack": "^4.7.0"
  }
}
